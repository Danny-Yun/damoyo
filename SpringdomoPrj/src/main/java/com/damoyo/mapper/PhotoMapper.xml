<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.rg/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.damoyo.mapper.PhotoMapper">
	<select id="getList" resultType="com.damoyo.domain.PhotocategoryVO">
		SELECT * FROM photo_category
			WHERE p_cate_name 
			ORDER BY p_cate_num DESC 
	</select>
	
	<insert id="insert">
		INSERT INTO photo(p_num, p_cate_name, p_image1, u_id)
		VALUES(p_num.nextval, #{p_cate_name}, #{p_image1}, #{u_id}) 
	</insert>
	
	<!-- delete 메서드를 연결해보겠습니다.
	#{bno} 로 가변코드부분을 메꿔줍니다. -->
	<delete id="delete">
		DELETE FROM photo WHERE p_num = #{p_num}
	</delete>
	
	<update id="update">
		UPDATE photo 
		SET p_cate_name=#{p_cate_name}, p_image1=#{p_image1}, 
		p_like=#{p_like}, p_date=sysdate WHERE p_num=#{p_num}
	</update>
	
	<select id="select" resultType="com.damoyo.domain.PhotoVO">
		SELECT * FROM photo
	</select>
	
	
</mapper>